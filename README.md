# PlaidTransactions
Use Plaid API to retrieve bank and card transactions. Will clean and export transactions on excel file.

To run:
- Create python virtual environment
- Install plaid-python, pandas, openpyxl, python-dotenv ('pip install plaid-python pandas openpyxl python-dotenv')
- Run Excel File
